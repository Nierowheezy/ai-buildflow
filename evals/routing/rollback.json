{
  "skill": "rollback",
  "positive": [
    { "prompt": "Run /rollback 4 because the export feature is corrupting files", "top_k": 1 },
    { "prompt": "Plan a safe reversal of the completed PDF export feature", "top_k": 3 },
    { "prompt": "Find the feature's archived spec and exact commit, then prepare an undo plan", "top_k": 3 }
  ],
  "negative": [
    { "prompt": "Run /fix for this new bug without undoing a completed feature", "owner": "fix" },
    { "prompt": "Run /complete for the rollback after it has been implemented and checked", "owner": "complete" }
  ]
}
